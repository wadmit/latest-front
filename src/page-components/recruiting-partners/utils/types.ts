export interface IWhyChooseType {
	title: string | React.ReactNode;
}

export interface ICardtype {
	title: string;
	desc: string;
	color: string;
}

export interface IBecomePartnerCardProp {
	title: string;
	desc: string;
	color: string;
	children: React.ReactNode;
}
